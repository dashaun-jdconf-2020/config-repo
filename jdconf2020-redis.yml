application:
  defaultMessage: this is using redis profile and sessions are enabled!
  dumpMessage: the hits are viewable in the logs!

management:
  endpoints:
    web:
      exposure:
        include: '*'